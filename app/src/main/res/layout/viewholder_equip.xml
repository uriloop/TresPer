<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="160dp"
    android:layout_height="160dp"
    android:orientation="vertical"
    android:paddingTop="10dp"
    android:maxWidth="340dp"
    android:layout_margin="10dp"
    android:minWidth="340dp"
    android:maxHeight="200dp"
    android:minHeight="180dp"
    android:layout_marginHorizontal="5dp"
    >

    <de.hdodenhof.circleimageview.CircleImageView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:elevation="12dp"
        android:layout_width="120dp"
        android:id="@+id/imatgeView"

        android:layout_gravity="center_horizontal"
        android:src="@drawable/ic_launcher_background"
        android:layout_height="120dp"
        app:civ_border_width="4dp"
        app:civ_border_color="@color/vermell_tauler"
        app:civ_border_overlay="true"/>

<!--    <ImageView
        android:id="@+id/imatgeView"
        android:layout_width="match_parent"
        android:maxHeight="200dp"
        android:maxWidth="200dp"
        android:layout_height="match_parent"
        android:src="@drawable/ic_launcher_background"

        />


<ImageView
    android:src="@drawable/frame_equip"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/frame_equip"
    android:layout_gravity="center"
    />-->
<ImageView
    android:src="@drawable/frame_equip_text"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/frame_equip_text"
    android:elevation="15dp"

    />


    <TextView
        android:id="@+id/nomEquipView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_horizontal|bottom"
        android:text="Nom Equip"
        android:textColor="@color/text_clar"
        android:maxLength="12"
        android:maxLines="1"
        android:elevation="18dp"
android:fontFamily="@font/sport_font"
        android:textSize="14sp"
        android:layout_marginBottom="8dp"

        />

</FrameLayout>